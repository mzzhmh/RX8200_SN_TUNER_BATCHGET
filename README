1. This project can do the data cleansing on the 2x parallels dumped raw data file and 
extract the site and IP information of all the Ericsson Sat Receivers from the dumped raw data file.


2. The getsn.py extracts the IP information from the raw file and uses html get to dump the configuration page of each Ericsson Sat Recv.
It parse the html file and output the SN and Audio tuner information to a .csv file.

3. The output will be saved in a .csv file.

4. Jenkinsfile is used for single-click deployment and it is used for running the task. 
It can be used by un-linux background staff to do the batch-get task.
